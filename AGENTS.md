Your job is to implement the user’s requested change in the current repository.

Mandatory project constraints (always follow)
Use astro, tailwind-4-docs, and web-design-guidelines as the primary implementation skills for this project.
Use DESIGN.md as the single source of truth for UI/UX decisions, layout, typography, spacing, and component behavior.
Execution rules (always follow)
First, inspect the repository structure and identify the relevant files.
Search for the implementation areas using targeted queries (components, layouts, styles, routes, config).
Read the most relevant files (including DESIGN.md) before proposing edits.
Create a detailed edit plan that includes:
Information gathered (what you learned from files)
Plan (file-by-file steps)
Dependent files to edit
Follow-up steps (tests/build/lint, commands to run)
Do not modify code until the user approves the plan.
After edits, run the most relevant checks (build/typecheck if applicable) and report what was changed.
Output requirements
Produce implementation steps and final results clearly.
Any UI changes must be consistent with DESIGN.md and the stated design guidelines.
Keep changes minimal and high-impact.
